{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/agency-os",
    "artifacts/api-server",
    "artifacts/mockup-sandbox",
    "lib/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@workspace/api-zod && npm run build --workspace=@workspace/db && npm run build --workspace=@workspace/agency-os && npm run build --workspace=@workspace/api-server && node -e \"const fs=require('fs'); fs.mkdirSync('dist', {recursive: true}); fs.cpSync('artifacts/api-server/dist', 'dist', {recursive: true}); if (fs.existsSync('artifacts/agency-os/dist/public')) fs.cpSync('artifacts/agency-os/dist/public', 'dist/public', {recursive: true});\"",
    "start": "SERVE_STATIC=true node dist/index.mjs",
    "dev": "node -e \"if (!require('fs').existsSync('dist/index.mjs')) require('child_process').execSync('npm run build', {stdio: 'inherit'});\" && SERVE_STATIC=true PORT=3000 node dist/index.mjs",
    "lint": "npm run build"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "alasql": "^4.17.3",
    "dotenv": "^17.4.2",
    "drizzle-zod": "^0.8.3",
    "jpeg-js": "^0.4.4",
    "openai": "^6.45.0",
    "p-limit": "^7.3.0",
    "p-retry": "^7.1.1",
    "pngjs": "^7.0.0",
    "zod": "^4.4.3",
    "zod-validation-error": "^5.0.0"
  }
}
